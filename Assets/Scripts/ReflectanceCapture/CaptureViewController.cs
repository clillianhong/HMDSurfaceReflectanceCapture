using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CaptureViewController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    /// <summary> 
    /// Creates a capture view object and adds it to the capture collection 
    /// </summary> 
    void CreateCaptureView(Texture2D texture, Transform camTransform, Transform lightTransform)
    {

    }
}
